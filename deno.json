{
  "workspace": [
    "./packages/bluesky",
    "./packages/mastodon"
  ],
  "imports": {
    "@hooksmith/core": "jsr:@hooksmith/core@^0.10.0",
    "@hooksmith/http": "jsr:@hooksmith/http@^0.10.0",
    "@std/assert": "jsr:@std/assert@^1"
  },
  "minimumDependencyAge": 0,
  "tasks": {
    "check": "deno fmt --check && deno lint && deno task check:docs && deno test -A && deno task check:examples",
    "check:docs": "deno run --allow-read=packages,extensions scripts/check_export_docs.ts",
    "check:examples": "deno run --allow-read --allow-run scripts/check_examples.ts",
    "test": "deno test -A"
  },
  "fmt": {
    "exclude": [
      "README.md"
    ]
  },
  "lock": false
}
